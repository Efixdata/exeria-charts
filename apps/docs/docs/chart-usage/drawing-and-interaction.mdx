---
sidebar_position: 5
title: Drawing and Interaction
description: Place trend lines and shapes in code, use the toolbar, and switch crosshair mode.
---

import { TutorialDrawingDemo } from "@site/src/components/TutorialChartDemo";
import DrawingToolShowcase from "@site/src/components/DrawingToolShowcase";

# Drawing and interaction

Charts are not read-only. Users (or your code) can **draw** on them — trend lines, levels, Fibonacci, text — and **interact** — crosshair, pan, zoom, select objects.

<TutorialDrawingDemo />

Two paths:

| Path | Best for |
| --- | --- |
| **Code** — `toolDrawer` | Alerts, saved layouts, AI-generated annotations |
| **Mouse** — ChartUI toolbar | Trading terminals, analyst workflows |

---

## Draw a trend line (code)

```ts
chart.toolDrawer.drawTrendLine({
  startStamp: candles[10].stamp,
  endStamp: candles[30].stamp,
  startPrice: candles[10].l,
  endPrice: candles[30].h,
  config: { color: "#5cc8ff" },
});
```

Pick two points from your candle array — start time/price and end time/price.

---

## Mark a time range

Highlight a session or event window:

```ts
chart.toolDrawer.drawTimeRange({
  text: "London session",
  startTime: candles[20].stamp,
  timeRange: 4 * 60 * 60 * 1000, // 4 hours in ms
  config: {
    color: "#f0b429",
    textColor: "#f0b429",
  },
});
```

---

## Generic shapes — `drawTool()`

When you need full control over anchors and type:

```ts
chart.toolDrawer.drawTool({
  type: "trendLine",
  color: "#14f7ab",
  width: 2,
  anchors: [
    { stamp: candles[5].stamp, offset: 0, value: candles[5].l, _index: 0 },
    { stamp: candles[15].stamp, offset: 0, value: candles[15].h, _index: 0 },
  ],
});
```

Every built-in type (`hLine`, `fibonLines`, `box`, …) is listed in the [Drawing tools catalog](../drawing-tools/catalog).

---

## Draw with the mouse (ChartUI)

Wrap your chart in `ChartUI` and use the **left toolbar** — no code required for each line.

Tutorial: [Drawing tools recipes](../tutorials/drawing-tools-recipes).  
Toolbar details: [React UI toolbar and tools](../advanced/react-ui-toolbar-and-tools).

---

## Try more shapes live

<DrawingToolShowcase visiblePresets={["trendLine", "hLine", "timeRange", "fibonLines"]} />

---

## Change interaction mode

Crosshair, pan, draw mode — controlled via the interactor:

```ts
const interactor = chart.getInteractor();
interactor.setMode("CROSSHAIR");
```

Use crosshair when clicks should read a price (e.g. [Trade from chart](../tutorials/trade-from-chart)).

---

## Remove a drawing

Keep the id returned when you create a shape:

```ts
const toolId = chart.toolDrawer.drawTrendLine({
  startStamp: candles[1].stamp,
  endStamp: candles[10].stamp,
  startPrice: candles[1].l,
  endPrice: candles[10].h,
});

if (toolId !== undefined) {
  chart.toolDrawer.deleteTool(toolId);
}
```

---

## ChartUI toolbars

| Bar | Location | Guide |
| --- | --- | --- |
| **Top toolbar** | Above the chart | Type, interval, indicators, scale, settings — [Top toolbar and mobile](./top-toolbar-and-mobile) |
| **Left menu** | Beside the chart (desktop) or pencil rail (mobile) | Drawing tools — [Drawing tools overview](../drawing-tools/overview) |
| **Chart settings** | Gear icon / ⋯ menu | Colors, layers, language — [Chart settings](./chart-settings) |

---

## Touch and mobile

On phones and tablets the chart supports:

| Gesture | Effect |
| --- | --- |
| **Pan** | Scroll along time |
| **Pinch** | Zoom |
| **Long press** | Context menu (go to start/end, toggle crosshair) |

Layout tips: [Mobile and responsive](../advanced/mobile-and-responsive).  
Consumer example: [Fintech integration demo](/starters/fintech-integration).

---

## Where to go deeper

- [Drawing tools overview](../drawing-tools/overview)
- [Trend line](../drawing-tools/trend-line)
- [Levels and channels](../drawing-tools/levels-and-channels)
- [Shapes and annotations](../drawing-tools/shapes-and-annotations)
