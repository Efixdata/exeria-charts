/**
 * Mobile menu is handled by `@site/src/components/MobileNav` in NavbarContent.
 * This swizzle is kept so Docusaurus does not fall back to the default sidebar toggle.
 */
export default function MobileSidebarToggle(): null {
  return null;
}
