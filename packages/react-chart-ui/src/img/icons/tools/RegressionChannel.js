import * as React from "react";
import { ChartLine } from "phosphor-react";

const RegressionChannel = (props) => (
  <ChartLine size={18} weight="fill" aria-hidden {...props} />
);

export default RegressionChannel;
