// @ts-nocheck
import * as React from "react";
import { IconButton } from "ui";
import { Share } from "../img/icons";

export const ShareChartButton = (props) => {
  const onClick = () => {

  };

  return (
    <IconButton onClick={onClick}><Share /></IconButton>
  )
};
